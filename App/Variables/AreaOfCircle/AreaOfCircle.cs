namespace App.Variables.AreaOfCircle;

/// <summary>
/// Обязательная задача: вычислить площадь круга по радиусу.
/// Формула: S = PI * r^2
/// Задание на тему "Переменные": используйте осмысленные имена и константы.
/// 
/// Это заготовка решения. Реализацию студент добавляет сам.
/// </summary>
public static class AreaOfCircle
{
    /// <summary>
    /// Возвращает площадь круга по заданному радиусу.
    /// Ожидаемое поведение (см. тесты):
    /// - radius >= 0: вернуть корректную площадь (double)
    /// - radius < 0: выбросить ArgumentOutOfRangeException
    /// </summary>
    public static double Calculate(double radius)
    {
        // Подсказка: используйте Math.PI и возведение в квадрат radius * radius.
        // Об ошибочных значениях: отрицательный радиус считается некорректным входом.
        throw new NotImplementedException();
    }
}
